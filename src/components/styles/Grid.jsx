import styled from 'styled-components';

const Grid = styled.div`
    display: grid;
    grid-template-columns: 1fr 4fr;
    margin-top: 5vh;
    height: 95vh;
    width: 100%;
`

export default Grid;